{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "uv run python -u -m app.analysis_worker --lottery 天天樂",
    "restartPolicyType": "NEVER",
    "cronSchedule": "3/5 * * * *"
  }
}
