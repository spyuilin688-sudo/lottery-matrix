{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "uv run python -u -m app.worker --lottery 天天樂 --scheduled",
    "cronSchedule": "3/5 * * * *",
    "restartPolicyType": "NEVER"
  }
}
