{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "uv run --no-dev --no-sync python -u -m app.analysis_worker --lottery 天天樂",
    "restartPolicyType": "NEVER",
    "cronSchedule": "10 10 * * *"
  }
}
