{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "uv run python -u -m app.worker --lottery 大樂透",
    "cronSchedule": "*/15 * * * *",
    "restartPolicyType": "NEVER"
  }
}
