{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "uv run --no-dev --no-sync python -u -m app.fantasy5_railway_job",
    "restartPolicyType": "NEVER",
    "cronSchedule": "33 1,2 * * *"
  }
}
