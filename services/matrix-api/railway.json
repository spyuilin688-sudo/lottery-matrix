{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "uv run --no-dev --no-sync python -u -m app.primary_worker --group evening",
    "restartPolicyType": "NEVER",
    "cronSchedule": "10 22 * * *"
  }
}
