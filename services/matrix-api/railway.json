{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "uv run python -u -m app.worker --lottery 今彩539 --scheduled",
    "cronSchedule": "3/5 * * * 1-6",
    "restartPolicyType": "NEVER"
  }
}
