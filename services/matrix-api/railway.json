{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "uv run python -m app.worker_all",
    "cronSchedule": "*/15 * * * *",
    "restartPolicyType": "NEVER"
  }
}
