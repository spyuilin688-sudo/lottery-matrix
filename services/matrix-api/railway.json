{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "uv run python -u -m app.primary_worker --group evening",
    "restartPolicyType": "NEVER",
    "cronSchedule": "10 22 * * *"
  }
}
