{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "uv run python -u -m app.worker",
    "cronSchedule": "*/15 * * * *",
    "restartPolicyType": "NEVER"
  }
}
