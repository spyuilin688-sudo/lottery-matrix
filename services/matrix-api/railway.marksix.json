{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "uv run python -u -m app.worker --lottery 六合彩",
    "cronSchedule": "*/15 * * * *",
    "restartPolicyType": "NEVER"
  }
}
