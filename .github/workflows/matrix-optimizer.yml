name: Matrix optimization candidates
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  inspect-code:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
      - name: Scan read-only optimization candidates
        run: node scripts/matrix-optimizer-code.mjs matrix-optimization-candidates.json
      - uses: actions/upload-artifact@v4
        with:
          name: matrix-optimization-candidates
          path: matrix-optimization-candidates.json
          retention-days: 90
