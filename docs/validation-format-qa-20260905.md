# 驗證過程修正與檢查紀錄

## 已修正

- 探索與天衍的驗證過程使用結果回應的彩種。僅變更探索設定時，期號格式、特別號、計算範圍與三欄排列維持原結果。
- 天衍重新展開時，從驗證區讀取結果彩種，再取得對應的版路與歷史資料。
- 鎖定條件的整組三欄交替使用 `#12243A`／`#0E1D30`，欄間與組間均透出頁面背景。
- 版路摘要的公式序列拆為 `+5`、`.`、`15`，合值序列加上獨立的 `合值`，相鄰節點間距為 1px。
- 天衍正式展開層沿用七碼自適應欄格及特別號 `+` 元件，並確實隱藏被取代的原摘要與驗證組，避免重複顯示。

主線既有的準4+預設連準已包含準5進6；共用提示與確認小窗已有 310 × 200px 規格。本次檢查其行為，不重複建立元件。

## 驗證

- 6 個相關單元測試檔、61 項測試通過。
- 4 個既有 Node CSS／產品契約測試檔、32 項測試通過。
- `npm run build:pages` 通過，包括 TypeScript 與 27 個受保護行動執行檔檢查。保留原有主 bundle 大於 500kB 的警告。
- `git diff --check` 通過。
- 手機測試頁：`/tests/validation-format-fixture.html`，使用實際探索、天衍元件及正式 CSS，資料全部由本地 fixture 提供。可選 320、390、430px。
- 探索今彩539 → 設定六合彩，以及大樂透 → 設定今彩539，未再探索時，驗證文字與欄寬不變；收合再展開仍使用原彩種。
- 天衍大樂透的正式 portal 已載入；變更設定為今彩539後，內容與欄寬不變，重新展開仍是大樂透。
- 奇、偶組每一欄的 computed background 分別為 `rgb(18,36,58)`、`rgb(14,29,48)`，整組外容器透明。
- 320px 天衍七碼列每列包含七顆號碼及 `+`，號碼列的 `scrollWidth === clientWidth`，最後一顆完整位於欄內。原驗證節點的 computed display 均為 `none`。
- 390px 提示與確認小窗動畫結束後均為 310 × 200px。摘要公式相鄰 token 的實測間距為 1px。

測試頁的歷史與預測數字為合成資料，不能用來驗證演算法命中率。此紀錄不代表已部署或真實六合彩資料已恢復。

## 六合彩空結果：尚待後端資料復原

2026-09-05 讀取正式資料得到：

- 最新可用開獎資料為 `026095`（2026-08-29）。SC888 頁面同時標記 `newestBetId = 026095`、下一期 `026096`。
- 分析版本 `026095:matrix-python-v12` 的工作狀態為 `complete`，完成時間為 2026-09-02 07:19:33 UTC。
- 同版本的 `matrix_explore_results`、`matrix_analysis_artifacts`、`matrix_analysis_artifact_chunks` 都沒有資料。
- 最新六合彩更新工作狀態為 `waiting_source`，來源期號與資料庫期號同為 `026095`。
- Railway API 健康檢查正常，回報版本與本次基準主線相同。

既有清理程式會刪除超過三天的結果、artifact 與 chunks，但保留分析工作狀態。這符合目前缺資料的現象；未取得刪除稽核紀錄，因此不能斷言實際刪除事件。既有 worker 對 complete 工作僅能從 artifact 還原結果；當 artifact 也不存在時直接返回，單純重跑同一工作無法恢復。

後續需處理 complete 但資料缺失的重建路徑，並檢查最新可用結果的保留條件，再使用既有 v12 演算法產生結果，最後確認探索 RPC 和驗證 RPC 均可回傳資料。本次沒有變更正式資料庫、開獎來源、演算法或生產排程，也沒有將前端修正宣稱為六合彩資料復原。
