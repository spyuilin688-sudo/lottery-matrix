# 權限管理開關

管理網站：https://matrix-permissions.spyuilin688.chatgpt.site

網站獨立部署於 Sites，僅限擁有者登入；不占用既有 AppDeploy 管理後台部署。

兩個開關由 Supabase private.matrix_permission_settings 保存：

| 開關 | 開啟 | 關閉 |
| --- | --- | --- |
| 顯示訂閱購買 | 顯示目前訂閱狀態、購買入口、方案、轉帳、付款紀錄及退款規範，恢復指定正式文案與完整篇章 | 套用下列查詢文案及篇章隱藏；隱藏上述卡片與入口（含訂閱金框及占位），相關頁面由 router 返回會員頁 |
| 註冊會員免費使用 | 所有有效的新舊註冊會員取得天衍、天工、探索七期、十三期、完整範圍 | 依原本方案、推薦、試用計算權限 |

PWA 首次需發布包含此串接的版本。之後變更設定不需要重新部署：開啟中的 PWA 在前景每 30 秒或重回前景時重讀；每次演算法請求在讀取快取前確認設定版本。伺服器 RPC 權限立即採用設定。

購買開關只控制介面顯示，不停止既有付款後端。免費開放不修改會員方案與到期日，不開放其他 Pro 功能。

管理憑證只保存在 Sites 的 MATRIX_MANAGEMENT_TOKEN 秘密環境變數；資料庫只保存 SHA-256。它只能修改這兩項設定，不能存取會員資料。不要將憑證放進 PWA、Git 或公開環境變數。
輪替時產生至少 48 隨機位元組的 token，更新 private.matrix_permission_credentials 的 hash 及 Sites runtime secret，再發布既有網站版本使秘密生效。

設定寫入需 expectedRevision，衝突回 409，管理頁必須重讀。操作失敗或結果未知時不顯示儲存成功。

本次直接相關驗證：

- node scripts/tests/permission-switches.test.mjs
- node node_modules/vitest/vitest.mjs run src/permission-settings.test.tsx src/__tests__/MemberProfilePage.test.tsx src/__tests__/MatrixExplorePage.test.tsx src/matrix-algorithm-api.test.ts src/matrix-explore-rpc.test.ts
- TypeScript 與 Vite 建置
- 正式資料庫 rollback 交易驗證會員權限回復、訪客限制與未授權更新拒絕

## 同一購買開關的文案與篇章範圍

關閉時按以下規格顯示；開啟時完整恢復原文。對所有訪客採用同一設定，不依爬蟲或來源切換。未列出的文字保持原樣；此設定不代表所有收費或分析相關文字都已移除。

| 位置 | 關閉時 |
| --- | --- |
| 首次使用提示 | 標題「使用教學」；內容「點擊右下方「我的」，再點擊「LINE 登入」即可使用查詢；首頁下方的 Matrix Core 進入探索。」；按鈕「立即登入」「知道了」。開啟中的提示亦隨設定更新，不重新彈出已讀提示。 |
| 推薦成功認定 | 隱藏第二、三項，保留第一項。 |
| 推薦獎勵補充規則（使用者稱補充說明） | 整段標題及內容隱藏；推薦成功獎勵保留。 |
| 啟動碼使用說明 | 隱藏第一項。 |
| 關於樂彩 | 「直覺且易於使用的開獎資料查詢與分析服務」→「直覺且易於使用的開獎資料查詢」；「整合各項分析工具」→「整合各項查詢工具」；「且容易使用的分析工具」→「且容易使用的查詢工具」；「並透過多項分析功能」→「並透過多項查詢功能」。 |
| 服務內容與使用說明 | 隱藏第七章標題及內容。 |
| 會員服務條例 | 第一章「Matrix 分析」→「Matrix 查詢」；隱藏第二、三、四、五、八、十章標題及內容，其他章節保留原編號。 |
| 隱私權政策 | 第一章第二、三項及第二章第二、三、五項隱藏。 |
| 聲明與免責事項 | 第一章「計算及分析工具」→「計算及查詢工具」；第二章「分析結果及探索結果僅供參考」→「查詢結果及探索結果僅供參考」；第三章「功能及分析結果」→「功能及查詢結果」；第七項「分析」→「查詢」。 |
| 探索／天衍／天工結果區 | 「預測期」→「查詢期」、「預測」→「結果」；天工「預測位置」→「查詢位置」。 |
| 展開版路驗證結果 | 「本期預測」→「版路結果」，包含共用驗證元件及探索預覽；數據與演算法不變。 |
| 指南 01 新手入門 | 隱藏基本導覽第五項及 Matrix Pro 區塊。 |
| 指南 14 Matrix Pro | 整類隱藏，保留其餘分類原始編號；若正在該分類，切換關閉後回到 01 新手入門。 |
| 指南 16 常見問題 | 隱藏「查看 Matrix Pro 權限」標題及內容。 |
| 指南 17 關於樂彩 Matrix | 「查詢與分析服務」→「查詢服務」；「各項分析工具」→「各項查詢工具」。 |

文案回復與結果區驗證：`src/__tests__/PublicCopySwitch.test.tsx`、`src/__tests__/ResultCopySwitch.test.tsx`。此變更沿用既有 Supabase 設定及更新週期，沒有新增開關或資料庫遷移。

- Matrix 狀態展開後左上角「預測：」→「結果：」，欄位「預測期」→「查詢期」、「預測」→「結果」；開啟購買開關恢復原文。
- 首頁五大功能下方、導覽上方增加使用者指定的兩段聲明，購買關閉時顯示、開啟時隱藏。
