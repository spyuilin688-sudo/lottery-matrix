# fix(admin): 背景執行手動更新並修正分析狀態

後台將 tianheng／tianshu 當成未知分析階段，導致整組 Railway 狀態被判定異常。手動更新同步等待爬蟲，會超過瀏覽器請求期限；天天樂則仍受舊的禁止抓取規則阻擋。

本次修正：
- 補齊天衡／天樞階段解析。
- 天天樂沿用既有 crawler，驗證已存入的期數，不啟動分析。來源尚未更新時明確回報。
- 手動更新改為背景工作與工作編號查詢；Supabase 每彩種僅保留一筆最新工作，原子受理防止多執行個體重複啟動，過期及舊工作不能覆寫完成結果。
- 後台兩個入口均支援狀態查詢、有限輪詢、撤權／離頁取消，以及查詢中斷後以原工作編號接續。
- RPC 僅供 service_role 使用，加入 API 清單及 Supabase Edge import map。

驗證：218 個限定 Vitest 測試、95 個限定 Python 測試通過，後台正式建置成功。獨立審查的兩項問題已修正並複查。未執行全專案測試。

型別檢查仍有基線既存診斷，本次相較初始 main 未增加。雲端瀏覽器阻擋本機預覽，因此變更後的瀏覽器視覺驗證尚未完成。

部署順序：先套用 `20260920100000_manual_refresh_jobs.sql`，再協調更新 Railway recovery/API、管理後台 Edge backend 與前端；回應格式由完成資料改為工作狀態，舊瀏覽器分頁需重新載入。尚未套用正式遷移或部署。

排程與快取優化不在本次變更範圍。
